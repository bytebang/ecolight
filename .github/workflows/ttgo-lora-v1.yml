# builds the hardware image for the ttgo-lora-v1 board
name: ttgo-lora-v1

on:
  push:
    branches: [ "main" ]
#    paths:
#      - 'hardware/**'
  pull_request:
    branches: [ "main" ]
    
jobs:
  build:
    uses: esphome/workflows/.github/workflows/publish.yml@main
    with:
      files: ecolight_ttgo-lora-v1.yml
      name: ecolight
